@model BarManegment.Areas.Admin.ViewModels.OathRequestCreateViewModel
@{
    ViewBag.Title = "تقديم طلب أداء اليمين";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid pt-4 px-4">
    <div class="row justify-content-center">
        <div class="col-lg-8">
            <div class="card shadow border-0">
                <div class="card-header bg-success text-white py-3">
                    <h5 class="mb-0 fw-bold"><i class="bi bi-patch-plus-fill me-2"></i> طلب أداء اليمين القانونية</h5>
                </div>
                <div class="card-body p-4">

                    <div class="alert alert-light border mb-4 d-flex align-items-center">
                        <i class="bi bi-person-circle fs-2 text-secondary me-3"></i>
                        <div>
                            <h6 class="mb-0 fw-bold">@Model.TraineeName</h6>
                            <small class="text-muted">رقم الملف: @Model.TraineeId</small>
                        </div>
                    </div>

                    @using (Html.BeginForm("Create", "OathRequests", FormMethod.Post, new { enctype = "multipart/form-data" }))
                    {
                        @Html.AntiForgeryToken()
                        @Html.HiddenFor(m => m.TraineeId)
                        @Html.HiddenFor(m => m.TraineeName)

                        @Html.ValidationSummary(true, "", new { @class = "alert alert-danger" })

                        <h6 class="fw-bold text-secondary border-bottom pb-2 mb-3">المرفقات المطلوبة</h6>

                        <div class="mb-4">
                            <label class="form-label fw-bold">1. نموذج انتهاء التمرين (موقع من المشرف) <span class="text-danger">*</span></label>
                            <input type="file" name="CompletionFormFile" class="form-control" required accept=".pdf,.jpg,.jpeg,.png" />
                            @Html.ValidationMessageFor(m => m.CompletionFormFile, "", new { @class = "text-danger small" })
                            <div class="form-text">يرجى رفع صورة واضحة أو ملف PDF للنموذج المعتمد.</div>
                        </div>

                        <div class="mb-4">
                            <label class="form-label fw-bold">2. شهادة حسن سيرة وسلوك من المشرف <span class="text-danger">*</span></label>
                            <input type="file" name="SupervisorCertificateFile" class="form-control" required accept=".pdf,.jpg,.jpeg,.png" />
                            @Html.ValidationMessageFor(m => m.SupervisorCertificateFile, "", new { @class = "text-danger small" })
                        </div>

                        <div class="alert alert-warning small">
                            <i class="bi bi-exclamation-triangle-fill me-1"></i>
                            بتقديم هذا الطلب، أنت تؤكد أن المتدرب قد استوفى جميع الشروط القانونية والأكاديمية.
                            سيتم مراجعة الطلب من قبل لجنة اليمين واحتساب الرسوم المستحقة.
                        </div>

                        <div class="text-end mt-4 pt-3 border-top">
                            <a href="@Url.Action("Details", "RegisteredTrainees", new { id = Model.TraineeId })" class="btn btn-secondary me-2">إلغاء</a>
                            <button type="submit" class="btn btn-success px-5 fw-bold">
                                <i class="bi bi-send-fill me-2"></i> تقديم الطلب
                            </button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>