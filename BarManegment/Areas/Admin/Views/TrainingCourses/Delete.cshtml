@model BarManegment.Models.TrainingCourse
@{
    ViewBag.Title = "حذف دورة تدريبية";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<div class="container-fluid pt-4 px-4">
    <div class="row justify-content-center">
        <div class="col-md-6">
            <div class="card shadow border-danger mb-4">
                <div class="card-header bg-danger text-white py-3">
                    <h5 class="mb-0 fw-bold"><i class="bi bi-exclamation-triangle-fill me-2"></i> تأكيد الحذف</h5>
                </div>
                <div class="card-body p-4 text-center">
                    <h4 class="text-danger mb-3">هل أنت متأكد من حذف هذه الدورة؟</h4>
                    <p class="text-muted mb-4">
                        سيؤدي هذا الإجراء إلى حذف الدورة <strong>"@Model.CourseName"</strong> وجميع الجلسات التدريبية المرتبطة بها نهائياً.
                    </p>

                    <div class="bg-light p-3 rounded border mb-4 text-start">
                        <dl class="row mb-0">
                            <dt class="col-sm-4">اسم الدورة:</dt>
                            <dd class="col-sm-8 fw-bold">@Model.CourseName</dd>

                            <dt class="col-sm-4">عدد الجلسات:</dt>
                            <dd class="col-sm-8">@(Model.Sessions?.Count ?? 0)</dd>
                        </dl>
                    </div>

                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()
                        <div class="d-flex justify-content-center gap-2">
                            <a href="@Url.Action("Index")" class="btn btn-secondary px-4">إلغاء</a>
                            <button type="submit" class="btn btn-danger px-4 fw-bold">
                                <i class="bi bi-trash-fill me-1"></i> تأكيد الحذف
                            </button>
                        </div>
                    }
                </div>
            </div>
        </div>
    </div>
</div>
