@model IEnumerable<BarManegment.Models.GraduateApplication>
@{
    ViewBag.Title = "إدارة حجوزات حصص المحامين";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
}

<h2><i class="bi bi-pause-circle-fill me-2"></i> @ViewBag.Title</h2>
<p class="text-muted">
    ابحث عن المحامي (بالاسم، رقم العضوية، أو الهوية) لعرض حصصه المستحقة وإدارة الحجوزات عليها (بسبب القروض أو أي مستحقات أخرى).
</p>
<hr />

@using (Html.BeginForm("Index", "ShareManagement", FormMethod.Get, new { @class = "row g-3 mb-3" }))
{
    <div class="col-md-8">
        <input type="text" name="searchString" class="form-control" placeholder="ابحث بالاسم، رقم العضوية، أو الهوية..." value="@ViewBag.SearchString" />
    </div>
    <div class="col-md-4">
        <button type="submit" class="btn btn-info w-100">
            <i class="bi bi-search"></i> بحث
        </button>
    </div>
}

<table class="table table-hover">
    <thead>
        <tr>
            <th>الاسم</th>
            <th>رقم العضوية</th>
            <th>الحالة</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.ArabicName</td>
                <td>@item.MembershipId</td>
                <td>@item.ApplicationStatus.Name</td>
                <td>
                    @Html.ActionLink("إدارة الحصص", "Manage", new { id = item.Id }, new { @class = "btn btn-primary" })
                </td>
            </tr>
        }
    </tbody>
</table>
