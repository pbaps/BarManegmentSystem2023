<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - نقابة المحامين الفلسطينيين</title>

    @* (جلب ستايل Bootstrap لضمان تنسيق الجداول والخطوط) *@
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">

    @* (استخدام خط Cairo للطباعة الرسمية) *@
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: 'Cairo', sans-serif;
            background-color: #e0e0e0; /* (لون خلفية خفيف لرؤية حدود الصفحة) */
        }

        /* (تحديد حاوية الطباعة بحجم A4) */
        .print-body-content {
            background-color: #fff;
            margin: 20px auto;
            padding: 20px;
            width: 21cm; /* (عرض ورقة A4) */
            min-height: 29.7cm; /* (ارتفاع ورقة A4) */
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            box-sizing: border-box;
        }

        /* (إعدادات الطباعة الفعلية) */
        @@media print {
            body {
                background-color: #fff; /* (إزالة الخلفية الرمادية عند الطباعة) */
                margin: 0;
                padding: 0;
            }

            .print-body-content {
                margin: 0;
                padding: 0;
                box-shadow: none;
                border: none;
                width: 100%;
                height: auto;
            }
            /* (إخفاء أي أزرار طباعة قد تكون في الواجهة) */
            .no-print {
                display: none !important;
            }
        }
    </style>

    @* (السماح لكل نموذج طباعة بإضافة تنسيقاته الخاصة) *@
    @RenderSection("styles", required: false)
</head>
<body>

    <div class="print-body-content">
        @* (هنا يتم عرض محتوى نماذج الطباعة مثل PrintApplicationForm.cshtml) *@
        @RenderBody()
    </div>

    @* (السماح لكل نموذج طباعة بإضافة سكربتات خاصة، مثل دالة الطباعة التلقائية) *@
    @RenderSection("scripts", required: false)
</body>
</html>
