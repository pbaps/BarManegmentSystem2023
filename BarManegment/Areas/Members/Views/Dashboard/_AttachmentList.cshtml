@model IEnumerable<BarManegment.Models.Attachment>

<div class="table-responsive">
    <table class="table table-striped table-hover table-bordered mb-0">
        <thead class="table-light">
            <tr>
                <th>نوع المرفق</th>
                <th>اسم الملف</th>
                <th>تاريخ الرفع</th>
                <th class="text-center" style="width: 100px;">إجراءات</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Any())
            {
                foreach (var item in Model)
                {
                    <tr>
                        <td>@item.AttachmentType.Name</td>
                        <td>@item.OriginalFileName</td>
                        <td>@item.UploadDate.ToString("yyyy-MM-dd")</td>
                        <td class="text-center">
                            <a href="@Url.Action("GetAttachmentFile", "Dashboard", new { area = "Members", id = item.Id })" target="_blank" class="btn btn-info btn-sm" title="فتح الملف">
                                <i class="bi bi-eye-fill"></i>
                            </a>
                            @using (Html.BeginForm("DeleteAttachment", "Dashboard", new { area = "Members", attachmentId = item.Id }, FormMethod.Post, new { @class = "d-inline" }))
                            {
                                @Html.AntiForgeryToken()
                                <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('هل أنت متأكد من رغبتك في حذف هذا المرفق؟');" title="حذف المرفق">
                                    <i class="bi bi-trash"></i>
                                </button>
                            }
                        </td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="4" class="text-center text-muted py-3">
                        <i class="bi bi-info-circle me-2"></i> لم يتم رفع أي مرفقات بعد.
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>

