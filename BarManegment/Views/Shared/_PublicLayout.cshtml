<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - نقابة المحامين الفلسطينيين</title>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;600;700&display=swap" rel="stylesheet">

    <style>

        body {
            font-family: 'Cairo', sans-serif;
            background-color: #e9ecef; /* Light gray background to make the page stand out */
        }
        /* New A4 style container */
        .page-container {
            background: white;
            padding: 2.5cm; /* Margins similar to A4 paper */
            margin: 2rem auto;
            max-width: 21cm; /* A4 width */
            box-shadow: 0 0 10px rgba(0,0,0,0.1);
            border: 1px solid #dee2e6;
        }
    </style>
</head>
<body>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <script src="~/Scripts/jquery-3.4.1.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    @RenderSection("scripts", required: false)


    <div id="ai-chat-widget" style="position: fixed; bottom: 30px; left: 30px; z-index: 9999; font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;">

        <div id="chat-box" class="shadow-lg" style="display: none; width: 350px; height: 500px; background: white; border-radius: 15px; overflow: hidden; flex-direction: column; border: 1px solid #ddd; margin-bottom: 15px;">

            <div class="bg-primary text-white p-3 d-flex justify-content-between align-items-center">
                <div>
                    <i class="bi bi-robot me-2"></i> مساعد النقابة الذكي
                </div>
                <button onclick="toggleChat()" class="btn btn-sm btn-link text-white text-decoration-none">
                    <i class="bi bi-x-lg"></i>
                </button>
            </div>

            <div id="chat-messages" class="p-3" style="flex: 1; overflow-y: auto; background-color: #f8f9fa;">
                <div class="d-flex mb-3">
                    <div class="bg-white border p-2 rounded shadow-sm text-dark" style="max-width: 80%; font-size: 0.9rem;">
                        مرحباً بك! 👋 أنا هنا لمساعدتك. اسألني عن أي شيء يخص النظام أو الإجراءات.
                    </div>
                </div>
            </div>

            <div class="p-3 bg-white border-top">
                <div class="input-group">
                    <input type="text" id="user-input" class="form-control" placeholder="اكتب سؤالك هنا..." onkeypress="handleKeyPress(event)">
                    <button class="btn btn-primary" onclick="sendMessage()">
                        <i class="bi bi-send-fill"></i>
                    </button>
                </div>
            </div>
        </div>

        <button onclick="toggleChat()" class="btn btn-primary rounded-circle shadow-lg d-flex align-items-center justify-content-center" style="width: 60px; height: 60px; transition: transform 0.2s;">
            <i class="bi bi-chat-dots-fill fs-3"></i>
        </button>
    </div>

    <script>
        function toggleChat() {
            var box = document.getElementById('chat-box');
            if (box.style.display === 'none') {
                box.style.display = 'flex';
            } else {
                box.style.display = 'none';
            }
        }

        function handleKeyPress(e) {
            if (e.key === 'Enter') {
                sendMessage();
            }
        }

        function sendMessage() {
            var input = document.getElementById('user-input');
            var message = input.value.trim();
            var messagesArea = document.getElementById('chat-messages');

            if (message === "") return;

            // 1. إضافة رسالة المستخدم للشاشة
            messagesArea.innerHTML += `
                <div class="d-flex justify-content-end mb-3">
                    <div class="bg-primary text-white p-2 rounded shadow-sm" style="max-width: 80%; font-size: 0.9rem;">
                        ${message}
                    </div>
                </div>`;

            input.value = "";
            messagesArea.scrollTop = messagesArea.scrollHeight;

            // 2. إضافة مؤشر الكتابة (جاري التحميل...)
            var loadingId = "loading-" + Date.now();
            messagesArea.innerHTML += `
                <div id="${loadingId}" class="d-flex mb-3">
                    <div class="bg-white border p-2 rounded shadow-sm text-muted fst-italic" style="font-size: 0.8rem;">
                        جاري التفكير... <i class="bi bi-three-dots"></i>
                    </div>
                </div>`;
            messagesArea.scrollTop = messagesArea.scrollHeight;

            // 3. إرسال الطلب للسيرفر
            $.ajax({
                url: '/AiAssistant/SendMessage', // تأكد أن المسار صحيح
                type: 'POST',
                data: { userMessage: message },
                success: function (response) {
                    // إزالة مؤشر التحميل
                    document.getElementById(loadingId).remove();

                    if (response.success) {
                        messagesArea.innerHTML += `
                            <div class="d-flex mb-3">
                                <div class="bg-white border p-2 rounded shadow-sm text-dark" style="max-width: 85%; font-size: 0.9rem; line-height: 1.5;">
                                    <i class="bi bi-robot text-primary me-1"></i> ${response.response}
                                </div>
                            </div>`;
                    } else {
                        messagesArea.innerHTML += `<div class="text-danger small text-center mb-2">${response.response}</div>`;
                    }
                    messagesArea.scrollTop = messagesArea.scrollHeight;
                },
                error: function () {
                    document.getElementById(loadingId).remove();
                    messagesArea.innerHTML += `<div class="text-danger small text-center mb-2">فشل الاتصال بالخادم.</div>`;
                }
            });
        }
    </script>
</body>
</html>
